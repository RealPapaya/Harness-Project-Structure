## Operating rules (routing block — keep; persona edits go above)
<!-- 中文：Hermes 薄索引。規則正本在 {{HOME}}\.agents\institution\，不要在此累積規則。 -->

READ FIRST every session, before any task (no triviality judgment — always):
1. `{{HOME}}\.agents\institution\core-rules.md` — always-on rules
2. `{{HOME}}\.agents\institution\INDEX.md` — then follow its triggers

Hermes-specific:
- Check the default model's cost before routine work — switch to the cheap tier
  in core-rules.md §2 if a paid per-token model is the default.
- This harness may be rarely used: trust the institution files over local
  memory. Anything durable you learn goes to
  `{{HOME}}\.agents\institution\lessons.md`, not only to Hermes MEMORY.md.
- In project directories, also read the project's AGENTS.md / CLAUDE.md
  (auto-injected).
